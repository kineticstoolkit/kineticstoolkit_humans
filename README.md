# kineticstoolkit_humans

A module that infer joint centres based on anthropometric data

This module is in development and is not ready for production use.

As a first application, and to set a starting point, we want to provide the required elements to recreate the conventional gait model (plugin gait).
